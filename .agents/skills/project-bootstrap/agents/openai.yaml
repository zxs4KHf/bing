interface:
  display_name: "Project Bootstrap"
  short_description: "Recover verified context for an existing repository"
  default_prompt: "Use $project-bootstrap to reconstruct this repository context before implementation."
